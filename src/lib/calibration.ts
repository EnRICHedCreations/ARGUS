import { createHash } from "node:crypto";import type{Observation,Outcome,Prediction}from"./types";function outcomeId(id:string){return createHash("sha256").update(`outcome\n${id}`).digest("hex").slice(0,32)}export function resolveDuePredictions(predictions:Prediction[],observations:Observation[],now=new Date()):Outcome[]{return predictions.filter(p=>p.status==="open"&&Date.parse(p.resolvesAt)<=now.getTime()).map(p=>{const start=Date.parse(p.predictedAt),end=Date.parse(p.resolvesAt);let evidence:Observation[]=[],actual=0,target=1;if(p.predictionType==="cross_domain_follow_through"&&p.details.entityName){const name=String(p.details.entityName).toLowerCase(),prior=new Set((p.details.sourceIds as string[]|undefined)??[]);evidence=observations.filter(o=>Date.parse(o.publishedAt)>start&&Date.parse(o.publishedAt)<=end&&`${o.title} ${o.summary}`.toLowerCase().includes(name)&&!prior.has(o.sourceId));actual=new Set(evidence.map(o=>o.sourceId)).size;}else{target=Number(p.details.threshold??0);evidence=observations.filter(o=>o.sourceId===p.subjectScope&&Date.parse(o.publishedAt)>start&&Date.parse(o.publishedAt)<=end);actual=evidence.length;}const outcome=actual>=target,y=outcome?1:0;return{id:outcomeId(p.id),predictionId:p.id,resolvedAt:now.toISOString(),outcome,actualValue:actual,targetValue:target,brierScore:Number(((p.probability-y)**2).toFixed(6)),absoluteError:Math.abs(actual-target),resolutionMethod:p.predictionType==="cross_domain_follow_through"?"deterministic_cross_domain_v1":"deterministic_source_activity_v1",evidenceObservationIds:evidence.map(o=>o.id),details:{subjectScope:p.subjectScope,probability:p.probability,horizonHours:p.horizonHours,entityName:p.details.entityName??null}}});}export function calibrationSummary(predictions:Prediction[],outcomes:Outcome[]){if(!outcomes.length)return{resolved:0,meanBrierScore:null,accuracy:null};return{resolved:outcomes.length,meanBrierScore:Number((outcomes.reduce((s,o)=>s+o.brierScore,0)/outcomes.length).toFixed(6)),accuracy:Number((outcomes.filter(o=>o.outcome).length/outcomes.length).toFixed(6))};}
