import{NextRequest,NextResponse}from"next/server";import{getInferences}from"@/lib/inference-store";import{getHypotheses,getHypothesisEvaluations,getPatterns}from"@/lib/reasoning-memory";import{getExpectationOutcomes,getExpectations}from"@/lib/expectation-memory";import{getSemanticDevelopments}from"@/lib/semantic-memory";import{getObservations,getOutcomes,getPredictions,getSignals}from"@/lib/memory";import{runStructuredQuery,type QueryKind}from"@/lib/query";
const allowed=new Set<QueryKind>(["summary","observations","signals","inferences","patterns","semantic_developments","hypotheses","hypothesis_evaluations","expectations","expectation_outcomes","predictions","outcomes"]);
export async function GET(request:NextRequest){try{const kind=(request.nextUrl.searchParams.get("kind")??"summary")as QueryKind;if(!allowed.has(kind))return NextResponse.json({error:"unsupported_query_kind",allowed:[...allowed]},{status:400});const source=request.nextUrl.searchParams.get("source")??undefined,rawLimit=request.nextUrl.searchParams.get("limit"),limit=rawLimit==null?undefined:Number(rawLimit);const[observations,signals,inferences,patterns,semanticDevelopments,hypotheses,hypothesisEvaluations,expectations,expectationOutcomes,predictions,outcomes]=await Promise.all([getObservations(),getSignals(),getInferences(),getPatterns(),getSemanticDevelopments(),getHypotheses(),getHypothesisEvaluations(),getExpectations(),getExpectationOutcomes(),getPredictions(),getOutcomes()]);return NextResponse.json({query:{kind,source:source??null,limit:limit??25},result:runStructuredQuery({kind,source,limit},{observations,signals,inferences,patterns,semanticDevelopments,hypotheses,hypothesisEvaluations,expectations,expectationOutcomes,predictions,outcomes}),epistemicContract:{observed_fact:"directly supported by collected observations",signal:"deterministic/statistical detection",inference:"derived interpretation; not an observed fact",pattern:"deterministically detected structure",semantic_development:"specific entity-centered cross-domain development ranked by surprise, specificity and consequence",hypothesis:"candidate explanation under competition",expectation:"machine-resolvable evidence expectation",prediction:"falsifiable future claim",outcome:"observed resolution of a prior prediction"},llmProvider:"none",gate:15})}catch(error){return NextResponse.json({error:String(error),gate:15},{status:500})}}
